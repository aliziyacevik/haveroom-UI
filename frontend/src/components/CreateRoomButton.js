import React from 'react';

import Button from 'react-bootstrap/Button';

function CreateRoomButton(props) {
        return (
        	<div className="index-btn">
        		<Button variant="outline-success" size="lg">									Create Room 
        		</Button>{' '}	
        	</div>
        );
}

export default CreateRoomButton;
